先阅读simple文件夹下的仿真代码尤其是流式求解的仿真代码，弄清楚流式求解中出来的f_domain\output_dataset\streaming_dataset_a_frequency_shellz中的数据是什么格式，如何用于diffusion中的训练和求解，阅读各个md文件确保使用的方式正确。

阅读get_pic中生成粗图的方法，同样的要生成V1粗图以及阅读粗图生成的各个数据的含义

根据diffusion中的diffusion\README.md文件构建diffusion训练模型，包括其中包括数据读取代码，训练代码，评价代码，config文件，PINN-like 物理约束等，使用的是conda环境中的diffusion环境，conda 环境中的comsol为仿真环境，conda环境中的get_pic为粗图生成环境。

本次构建模型使用的数据集为f_domain\output_dataset\streaming_dataset_a_frequency_shell，40个小型数据集，仅用来测试数据流是否正确模型是否能够正常训练与运行，后续会变为1000+的大规模数据集，使用A6000进行训练，现在构建的代码仅用来做运行测试，不用好的结果。

训练时需要注意加上tqdm进度条以及断点续跑的功能，同时训练过程loss等可能在后续用到的数据都需要记录下来后续可能会用matplotlib进行绘图分析
