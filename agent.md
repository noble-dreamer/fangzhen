# Agent 工程协作规范

执行任何开发任务时，必须遵守以下规则。

## 1. 增量修改

- 每次修改必须小步进行，新增与删除的代码总量不得超过 **200 行**。
- 单个实现方案涉及的文件不得超过 **5 个**。
- 如果预计超过上述限制，必须拆分任务或改用影响范围更小的方案。
- 优先修改现有文件，非必要不得新增文件。
- 禁止进行与当前任务无关的重构、格式化、依赖升级或代码清理。
- 修改前先检查现有实现，优先复用项目已有的工具、组件和设计模式。

## 2. Git 工作流

- 必须使用 Git 管理代码，并遵循项目现有的 Gitflow 分支规范。
- 开始开发前，确认当前分支和工作区状态，避免覆盖或提交他人的修改。
- 每完成一个可独立验证的小功能点，必须按以下顺序执行：
  1. 运行相关测试、构建或静态检查；
  2. 确认验证通过；
  3. 检查变更范围；
  4. 立即创建一次独立 Commit。
- Commit 必须保持单一职责，不得混入无关修改。
- Commit Message 应简洁说明变更目的，并遵循项目已有规范。
- 未经明确授权，不得执行强制推送、历史重写、删除分支、重置他人修改等破坏性 Git 操作。

## 3. 实施原则

- 修改前先给出简短计划，说明预计修改的文件、改动范围和验证方式。
- 优先选择最小改动、低风险、易回滚的实现方案。
- 保持现有代码风格、目录结构、接口兼容性和工程约定。
- 不得擅自修改与需求无关的行为。
- 如果需求与本规范冲突，必须暂停实施并说明冲突点，不得自行放宽限制。

## 4. 完成标准

任务仅在满足以下条件后视为完成：

- 功能符合需求；
- 相关测试、构建或检查已通过；
- 单次改动不超过 200 行；
- 实现方案影响不超过 5 个文件；
- 每个独立功能点均已提交；
- 工作区中不存在由本任务产生但未说明的改动。

完成后应汇报：

- 修改内容；
- 涉及文件；
- 验证结果；
- Commit Hash；
- 已知限制或后续建议。
